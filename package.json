{
  "name": "activitypub-objects",
  "version": "0.0.3",
  "main": "src/index.ts",
  "author": "aoinu",
  "license": "MIT",
  "private": false,
  "repository": {
    "type": "git",
    "url": "https://github.com/Aoinu/activitypub-objects.git"
  },
  "scripts": {
    "lint": "node_modules/.bin/eslint './src/**/*.ts' './__tests__/**/*.ts'",
    "lint:fix": "node_modules/.bin/eslint --fix './src/**/*.ts' './__tests__/**/*.ts'",
    "test": "jest",
    "test:cov": "jest --coverage",
    "test:ci": "jest --ci --no-cache --coverage --reporters=default --reporters=jest-junit",
    "posttest:ci": "codecov",
    "build": "tsc",
    "prepare": "npm run build"
  },
  "devDependencies": {
    "@types/jest": "^25.2.3",
    "codecov": "^3.7.0",
    "jest": "^26.0.1",
    "jest-junit": "^10.0.0",
    "ts-jest": "^26.1.0",
    "typescript": "^3.9.3",
    "eslint": "^7.0.0",
    "eslint-plugin-prettier": "^3.1.3"
  },
  "keywords": [
    "activitypub"
  ]
}